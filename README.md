# 🍲 Fadak Food Panel (Rail Pardaz No Afarin)

یک سامانه جامع و هوشمند برای مدیریت رزرو، توزیع و پایش کیفیت ناهار پرسنل شرکت **ریل‌پرداز نُوآفرین** که با معماری مدرن و رویکرد مدیریت متمرکز شعب طراحی شده است.

---

## 🚀 ویژگی‌های کلیدی (Key Features)

* **مدیریت چند‌سازمانی (Multi-Branch Support):** تفکیک کامل منوها و دسترسی‌ها بر اساس شعب (مانند بخارست و راه‌آهن) به طوری که هر شعبه در یک تاریخ یکسان می‌تواند منوی کاملاً متفاوتی داشته باشد.
* **پروفایل هوشمند پرسنلی:** امکان مدیریت مشخصات فردی، تصویر آواتار با کادر افقی بهینه‌سازی شده و امنیت تغییر رمز عبور با سیستم تاییدیه دو مرحله‌ای و قابلیت نمایش/مخفی‌سازی رمز.
* **رزرو منوی روزانه:** مشاهده منو در تقویم شمسی به همراه وضعیت لحظه‌ای رزروها و محدودیت‌های زمانی تعریف شده توسط ادمین برای روزهای غیرفعال.
* **نظام بازخورد و رتبه‌بندی (Survey System):** قابلیت ثبت امتیاز و نظر برای هر وعده غذایی جهت پایش کیفیت کترینگ و ارائه گزارش‌های تحلیلی به مدیریت.
* **امنیت پیشرفته:** استفاده از پروتکل **JWT** و مکانیزم **Refresh Token** برای پایداری طولانی‌مدت و امنیت نشست‌های کاربری.
* **تجربه کاربری Mobile-First:** طراحی کاملاً واکنش‌گرا (Responsive) با استفاده از Tailwind CSS برای کاربری آسان و سریع روی گوشی‌های هوشمند.

---

## 🛠 پشته تکنولوژی (Tech Stack)

| بخش | تکنولوژی | نسخه |
| :--- | :--- | :--- |
| **Backend** | Python / Django | 3.12 / 5.2.7 |
| **API Architecture** | Django REST Framework | 3.16.1 |
| **Frontend** | React / Framer Motion | Latest |
| **Styling** | Tailwind CSS | 3.x |
| **Database** | PostgreSQL / SQLite | Production / Dev |
| **Cloud Hosting** | Liara Cloud | Platform-as-a-Service |

---
## 🏗️ ساختار پروژه (Project Structure)

این پروژه از دو بخش مجزای Backend (جنگو) و Frontend (ری‌اکت) تشکیل شده است:

```text
├── 📂 dashboard/              # اپلیکیشن اصلی منطق برنامه (Backend App)
│   ├── admin.py               # سفارشی‌سازی پنل مدیریت و فیلترینگ شعب
│   ├── models.py              # طراحی دیتابیس (User, Menu, Organization, Feedback)
│   ├── serializers.py         # تبدیل مدل‌ها به JSON برای فرانت‌اِند
│   ├── urls.py                # مسیریابی داخلی APIهای داشبورد
│   ├── views.py               # پیاده‌سازی منطق فیلترینگ هوشمند و گزارشات
│   └── 📂 migrations/         # تاریخچه تغییرات دیتابیس (از 0001 تا 0010)
│
├── 📂 food_panel/             # تنظیمات هسته مرکزی پروژه (Django Core)
│   ├── settings.py            # پیکربندی JWT، CORS، دیتابیس و تنظیمات لیارا
│   └── urls.py                # نقشه اصلی مسیریابی کل پروژه
│
├── 📂 frontend/               # رابط کاربری سمت کاربر (React App)
│   ├── 📂 src/
│   │   ├── api.js             # پیکربندی Axios و مدیریت اتصال به بک‌اِند
│   │   ├── 📂 components/     # قطعات UI (Navbar, MobileBottomNav, FoodCard)
│   │   ├── 📂 pages/          # صفحات اصلی (Menu, Profile, Feedback, Orders)
│   │   └── 📂 guards/         # محافظت از مسیرها (PrivateRoute, OrdersGuard)
│   └── .env.production        # تنظیمات متغیرهای محیطی برای سرور لیارا
│
└── 📂 Root Files/             # تنظیمات استقرار و ابزارهای سیستمی
    ├── liara.json             # تنظیمات دیسک و پلتفرم ابری لیارا
    ├── liara_nginx.conf       # پیکربندی وب‌سرور Nginx برای فایل‌های Media
    ├── requirements.txt       # لیست تمام کتابخانه‌های پایتونی پروژه
    └── export_data.py         # اسکریپت خروجی گرفتن هوشمند از دیتابیس)
```

 ### برای اجرای پروژه در سیستم خود، مراحل زیر را به ترتیب دنبال کنید:
 #### 1.راه‌اندازی بخش بک‌اِند (Backend - Django)
 ---

 ```text

 ۱. ساخت محیط مجازی (Virtual Environment)
python -m venv venv

 ۲. فعال‌سازی محیط مجازی
در ویندوز (Windows):
venv\Scripts\activate
در مک یا لینوکس (Mac/Linux):
source venv/bin/activate

 ۳. نصب بسته‌های نرم‌افزاری مورد نیاز
pip install -r requirements.txt

۴. بروزرسانی ساختار دیتابیس
python manage.py migrate

 ۵. اجرای سرور توسعه
python manage.py runserver

 ```
## 2.راه‌اندازی بخش فرانت‌اِند (Frontend - React)

```text
 ۱. ورود به پوشه فرانت‌اِند
cd frontend

 ۲. نصب کتابخانه‌های مورد نیاز (NPM)
npm install

 ۳. اجرای پروژه روی مرورگر
npm start

---
```
## ☁️ استقرار و میزبانی (Deployment)

این سامانه به طور کامل برای پلتفرم ابری **Liara** بهینه‌سازی شده است. جزئیات استقرار به شرح زیر است:

* **مدیریت فایل‌ها:** تنظیمات مربوط به دیسک‌های نوری جهت ذخیره‌سازی دائمی تصاویر پروفایل و آواتار پرسنل در فایل `liara.json` لحاظ گردیده است.
* **پیکربندی سرور:** وب‌سرور **Nginx** جهت مدیریت بهینه فایل‌های استاتیک و رسانه‌ای از طریق فایل `liara_nginx.conf` تنظیم شده است.
* **انتقال داده‌ها:** اسکریپت اختصاصی `export_data.py` جهت تهیه نسخه پشتیبان و انتقال امن اطلاعات دیتابیس طراحی شده است.

---

## 👨‍💻 تیم توسعه و ارتباطات

* **توسعه‌دهنده اصلی:** محمد علی
* **سازمان:** شرکت ریل‌پرداز نُوآفرین
* **هدف پروژه:** ارتقای سطح رفاهی و هوشمندسازی فرآیندهای داخلی سازمان

---
